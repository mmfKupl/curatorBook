import { TranslateKeysPipe } from './translate-keys.pipe';

describe('TranslateKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
